{
  "name": "wear_house",
  "private": true,
  "version": "1..0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "rimraf node_modules && rimraf packages/*/node_modules",
    "start": "turbo run start"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "rimraf": "^5.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "pg": "^8.14.0"
  }
}